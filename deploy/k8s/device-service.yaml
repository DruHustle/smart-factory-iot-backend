apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-service
  namespace: smart-factory
spec:
  replicas: 2
  selector:
    matchLabels:
      app: device-service
  template:
    metadata:
      labels:
        app: device-service
    spec:
      containers:
      - name: device-service
        image: smartfactory.azurecr.io/device-service:latest
        ports:
        - containerPort: 80
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: connection-string
---
apiVersion: v1
kind: Service
metadata:
  name: device-service
  namespace: smart-factory
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: device-service
