apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: smart-factory
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
      - name: identity-service
        image: smartfactory.azurecr.io/identity-service:latest
        ports:
        - containerPort: 80
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: connection-string
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: smart-factory
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: identity-service
