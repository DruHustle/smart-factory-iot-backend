apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: smart-factory
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
      - name: analytics-service
        image: smartfactory.azurecr.io/analytics-service:latest
        ports:
        - containerPort: 80
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: connection-string
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: smart-factory
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: analytics-service
